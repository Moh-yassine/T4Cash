import React from 'react';
import { View } from 'react-native';

/**
 * Composant placeholder pour un éventuel ticker type TradingView.
 * N'affiche rien pour l'instant ; supprimez l'import dans HomeScreen si vous ne l'utilisez pas.
 */
export function TradingViewTicker() {
  return <View />;
}
